import std.schema as schema

fn main():
    let user = schema.object({"email": schema.req(schema.string(true, 3, 120))})
    let bad = schema.parse(user, {"email": "  "})
    if bad.is_err():
        panic(bad.unwrap_err()["code"])

